spring.datasource.url=jdbc:mysql://localhost:3306/demo?useSSL=false
spring.datasource.username=root
spring.datasource.password=SQL64pns1vb.

##Hibernate properties
#This SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#Hibernate auto ddl (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

#for monitoring hibernate generated SQLs on console
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE
